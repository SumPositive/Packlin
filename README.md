# PackList
モチメモ  V3

## Features
- Swiping and drag actions restored using `List` and `EditButton`
- Pack rows are now regular list rows, so swipe actions and reordering work for entire packs (double-tap to edit)
